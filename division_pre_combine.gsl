.template 0

stylefn = 'styles/$(projectnode.name)__css.gsl'

if file.exists(global.project_path + stylefn)
. template 1
. cached = cache__open(stylefn)
. gsl from global.project_path + stylefn
. cache__close(cached)
. endtemplate
endif

.endtemplate