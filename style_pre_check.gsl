.template 0

if defined(projectnode.parent()) & name(projectnode.parent()) = 'dir'
  parentdir = projectnode.parent()
  if defined(parentdir.path)
    define projectnode.path = parentdir.path + '/'
  endif
else
  define projectnode.path = ''
endif

.endtemplate